FROM node:17-alpine

ARG NPM_VGS_PUBLISH_TOKEN
ARG LIB_VERSION

ENV NPM_VGS_PUBLISH_TOKEN=$NPM_VGS_PUBLISH_TOKEN
ENV LIB_VERSION=$LIB_VERSION

WORKDIR /src

ENTRYPOINT ["sh", "docker/build_and_publish.sh"]